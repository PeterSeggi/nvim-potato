- Fix the commenting plugin with ctrl /
- Add auto insert mode on new file
- Fix the gitsigns commands
- Config nvim tree
- Fix the c auto format
- Add xml lsp

Dependecies:
  npm v>=12.0
  ripgrep


sudo apt install npm
sudo apt-get install ripgrep
